*** JavaScript notes ***

- JavaScript is a programming language,
- Console work on REPL ( Read - Evalute - Print - Loop )
console.log : To write(log) the message on the console.

- Console code was not permanent
- Variables : A Variables is simply the name of a storage location
- Datatype : 1. Number
             2. Boolean
             3. String
             4. Undefined
             5. Null
             6. Bigint
             7. Symbol
syntax of declaring vaariable : 1. let (  )
                                2. var (  )
                                3. const (  )

pre-increment : (change, then use)
post-increment : (use, then change)
all javascript variable must be identified with unique names (identifiers)
Boolean represents a truth value -> true or false
undefined : A variable that hhas not been assigned a value is of type undefined.
null : the null value represent the intentional absence of anyy object value.
